# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=*

# PostgreSQL (postgresql://USER:PASSWORD@HOST:PORT/NAME)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/communityboard

# JWT (must match previously issued tokens if migrating in place)
JWT_SECRET=communityboard-secret-key-amalitech-2024
JWT_EXPIRATION_MS=86400000

# CORS - comma-separated list of allowed origins; unset = allow all (dev default)
CORS_ALLOWED_ORIGINS=http://localhost:3000
